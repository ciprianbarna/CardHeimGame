package game;

public class Objeto extends Carta {
    // Atributos

    // Métodos
    /**Constructor
     *
     * @param nombre - nombre de la carta
     * @param efecto - descripción de la carta
     */
    public Objeto(String nombre, String efecto) {
        super(nombre, efecto);
    }
}
